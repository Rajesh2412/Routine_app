rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This rule allows anyone with your Firebase config to read and write.
    // This is good for development, but you should secure this for a real app.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}